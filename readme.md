setup commands :
pkg update -y && pkg upgrade -y
cd ~ && git clone https://github.com/mrdanuwar32/termux_extrakeys_onoff/
mv keys ~
mv off ~/.termux
chmod +x keys

usage:
run this commond to turn on and off the extra keys of termux:
keys
or, bash keys

{ sometimes app lagg and dosent turn on or off, [ its rare ]
  you can exit app and re-open, and type "keys" again, it works !!
